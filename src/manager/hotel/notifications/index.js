import { Box } from "@mui/material";
import PageNotFinished from "../../pageNotFinished";
const Notifications = () => {
    return (
        <PageNotFinished />
    )
};

export default Notifications; 